#!/usr/bin/env bash
#
# Description: checks if the input is a false value.
# Usage: is-false [value]
#
set -Eeuo pipefail
shopt -s inherit_errexit

[[ $# -ne 1 ]] && exit 1

readonly T="${1^^}"

case "$T" in
N | NO | F | FALSE | OFF | 0) exit 0 ;;
esac

exit 1
