#!/usr/bin/env bash
#
# Description: checks if the input is a true value.
# Usage: is-true [value]
#
set -Eeuo pipefail
shopt -s inherit_errexit

[[ $# -ne 1 ]] && exit 1

readonly T="${1^^}"

case "$T" in
Y | YES | T | TRUE | ON | 1) exit 0 ;;
esac

exit 1
